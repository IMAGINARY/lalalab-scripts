#!/usr/bin/env bash

EXHIBIT="$(get-default-exhibit-by-hostname)"
if command -v "init-$EXHIBIT" > /dev/null
then
    exec "init-$EXHIBIT"
else
    echo "No initialization script found for $EXHIBIT"
fi
